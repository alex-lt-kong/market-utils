[pytest]
# We deliberately stay on httpx for the Starlette TestClient (see requirements-dev.txt);
# silence its benign "install httpx2 instead" deprecation so normal runs stay clean.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:Warning
