# Pinned to a tested set (compatible-release ~=): bounds resolution so a fresh
# install can't pull an untested major/minor. Bump deliberately, re-run tests.
fastapi~=0.138.0
starlette~=1.3.0          # transitive via fastapi; pinned (TestClient stack)
uvicorn[standard]~=0.49.0
jinja2~=3.1.6
itsdangerous~=2.2.0
apscheduler~=3.11.2
yfinance~=1.4.1
requests~=2.32.4
pandas~=3.0.3
lxml~=6.1.1
